#ifndef COMPARE_H
#define COMPARE_H

#define EQ(a, b) ((a) == (b))   // equal
#define LT(a, b) ((a) < (b))    // less than
#define LQ(a, b) ((a) <= (b))   // less or equal
#define GT(a, b) ((a) > (b))    // greater than
#define GQ(a, b) ((a) >= (b))   // greater or equal

#endif